{
  "$schema": "https://railpack.com/railpack.schema.json",
  "id": "gbaabw-backend",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run build",
    "startCommand": "cd backend && node dist/index.js",
    "watchPatterns": ["backend/**"]
  }
}
